# 移动端复古游戏机 - 规格文档

## 1. 功能概述

手机浏览器打开网页 → 检测横屏 → 显示模拟器界面 → 点击"选择卡带"加载游戏

## 2. 页面流程

```
┌─────────────────┐
│   打开网页      │
└────────┬────────┘
         │
    ┌────▼────┐
    │ 检测设备  │
    └────┬────┘
         │
    ┌────▼────┐      ┌─────────────────┐
    │ PC浏览器? │ ───→ │ 提示：请用手机打开 │
    └────┬────┘      └─────────────────┘
         │
    ┌────▼────┐
    │手机竖屏?  │ ───→ │ 请横屏后游玩 │
    └────┬────┘
         │
    ┌────▼────┐
    │ 横屏状态  │ ───→ 显示模拟器界面
    └─────────┘
```

## 3. 模拟器界面布局

### 加载前（选择卡带）
```
┌─────────────────────────────────────────────┐
│  [卡带槽1: 未插入]              [选择卡带]   │  ← 加载前显示
├─────────────────────────────────────────────┤
│                                              │
│  ┌────────┐ ┌─────────────────┐ ┌────────┐  │
│  │        │ │                 │ │        │  │
│  │  方向键 │ │   点击选择卡带   │ │  A/B键 │  │
│  │  (左侧) │ │    开始游戏      │ │ (右侧) │  │
│  │        │ │                 │ │        │  │
│  └────────┘ └─────────────────┘ └────────┘  │
│                                              │
└─────────────────────────────────────────────┘
```

### 加载后（游戏进行中）
```
┌─────────────────────────────────────────────┐
│                                              │
│                                              │
│  ┌────────┐ ┌─────────────────┐ ┌────────┐  │
│  │        │ │                 │ │        │  │
│  │  方向键 │ │   游戏画面 4:3  │ │  A/B键 │  │
│  │  (左侧) │ │     完全展示     │ │ (右侧) │  │
│  │        │ │                 │ │        │  │
│  └────────┘ └─────────────────┘ └────────┘  │
│                                              │
└─────────────────────────────────────────────┘
```

### 布局说明
- **加载前**：顶部显示卡带槽+选择按钮，两侧显示虚拟按键
- **加载后**：顶部按钮隐藏，屏幕最大化显示游戏画面
- 两侧按键始终显示，支持游戏操作
- 游戏画面 4:3 比例居中

## 4. 交互逻辑

### 4.1 卡带选择
- 点击"选择卡带"按钮 → 弹出ROM列表
- 选择ROM → 自动加载到卡带槽1
- 加载完成后自动开始游戏

### 4.2 横屏检测
- 进入页面检测orientation
- 竖屏显示遮罩："请横屏后游玩"
- 横屏后移除遮罩，显示模拟器

### 4.3 设备检测
- PC User-Agent 显示提示页
- 移动端正常进入

## 5. 技术实现

### 5.1 依赖
- EmulatorJS CDN: https://cdn.emulatorjs.org/
- 无需后端，纯静态页面

### 5.2 核心代码结构
```html
<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <script src="https://cdn.emulatorjs.org/stable/data/loader.js"></script>
</head>
<body>
  <!-- 横屏提示遮罩 -->
  <div id="portrait-warning">请横屏后游玩 ↻</div>
  
  <!-- 顶部栏 -->
  <div class="top-bar">
    <span id="cartridge-slot">卡带槽1: 未插入</span>
    <button onclick="showRomList()">选择卡带</button>
  </div>
  
  <!-- 模拟器容器 -->
  <div id="game-container"></div>
  
  <!-- 虚拟按键 -->
  <div class="virtual-controls">...</div>
  
  <!-- ROM列表弹窗 -->
  <div id="rom-modal" class="hidden">
    <button onclick="loadRom('rom1')">超级玛丽</button>
  </div>
</body>
</html>
```

## 6. ROM 列表（演示）

```javascript
const romList = [
  {
    id: 'rom1',
    name: '超级玛丽 (Super Mario)',
    url: 'https://example.com/roms/super-mario.nes',
    system: 'NES'
  }
];
```

## 7. 待实现

- [ ] 设备检测 + 提示
- [ ] 横屏检测 + 遮罩
- [ ] EmulatorJS 嵌入
- [ ] 卡带选择UI
- [ ] 虚拟按键（可选，触屏控制）
- [ ] 第一个ROM加载测试

## 8. 文件结构

```
/emulator-mobile/
  index.html    # 主页面
  games.json    # 游戏ROM列表
  style.css     # 样式
  app.js        # 逻辑
```
